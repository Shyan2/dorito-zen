import { createContext } from 'react';

export const ResolutionValueContext = createContext(null);
// Temperature, Humidity, CO2 etc.
export const SelectedPropertyIdContext = createContext(null);
export const ShowHeatMapContext = createContext(null);
