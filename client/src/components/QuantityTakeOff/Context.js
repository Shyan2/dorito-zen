import { createContext } from 'react';

export const LevelsContext = createContext(null);
export const QuantityContext = createContext(null);
export const SelectedElementsContext = createContext(null);
